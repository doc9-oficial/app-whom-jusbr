{
  "name": "app-whom-jusbr",
  "version": "1.0.0",
  "description": "App DocGo para integração com o Jusbr utilizando o Whom",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prebuild": "npm run clean",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "docgo-sdk": "github:doc9-oficial/docgo-sdk-js"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
